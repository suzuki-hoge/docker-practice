"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    Object.defineProperty(o, k2, { enumerable: true, get: function() { return m[k]; } });
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.exec = void 0;
var arg_1 = __importDefault(require("arg"));
var safe_1 = __importDefault(require("colors/safe"));
var messages_1 = require("../lib/messages");
var books_1 = require("../lib/books");
var Log = __importStar(require("../lib/log"));
function parseArgs(argv) {
    try {
        return arg_1.default({
            // Types
            '--format': String,
            '--help': Boolean,
            // Alias
            '-h': '--help',
        }, { argv: argv });
    }
    catch (e) {
        if (e.code === 'ARG_UNKNOWN_OPTION') {
            console.log(safe_1.default.red(messages_1.invalidOptionText));
        }
        else {
            console.log(safe_1.default.red('エラーが発生しました'));
        }
        console.log(messages_1.listBooksHelpText);
        return null;
    }
}
var bookFormatters = {
    tsv: function (bookMeta) {
        return bookMeta.slug + (bookMeta.title ? '\t' + bookMeta.title : '');
    },
    json: function (bookMeta) { return JSON.stringify(bookMeta); },
};
var exec = function (argv) {
    var args = parseArgs(argv);
    if (!args)
        return;
    if (args['--help']) {
        console.log(messages_1.listBooksHelpText);
        return;
    }
    var format = args['--format'] || 'tsv';
    if (!(format in bookFormatters)) {
        Log.error("format\u306E\u5024\uFF08" + format + "\uFF09\u304C\u4E0D\u6B63\u3067\u3059\u3002\"tsv\" \u307E\u305F\u306F \"json\" \u3092\u6307\u5B9A\u3057\u3066\u304F\u3060\u3055\u3044");
        return;
    }
    var formatter = bookFormatters[format];
    var bookMetaList = books_1.getLocalBookMetaList();
    var output = (bookMetaList === null || bookMetaList === void 0 ? void 0 : bookMetaList.length)
        ? bookMetaList.map(function (book) { return formatter(book); }).join('\n')
        : 'No books yet';
    console.log(output);
};
exports.exec = exec;
